public enum SpecHarvesterDocs {
    public static let summary = """
    SpecHarvester turns public repository metadata into reviewable SpecPM \
    candidate packages through a bounded, zero-trust pipeline.
    """
}
