# P31-T3 Real Selected Candidate Handoff Proposal

This page mirrors the P31-T3 dry-run artifact generated by
`selected-candidate-handoff-proposal`.

The machine-readable fixture is:

```text
tests/fixtures/selected_candidate_handoff_proposal/p31-t3-real-selected-candidate-handoff.example.json
```

The Markdown companion is:

```text
docs/SELECTED_CANDIDATE_HANDOFF_PROPOSAL_P31_T3.md
```

## Source

The helper consumed the recorded P30-T5 selected handoff dry-run evidence:

```text
tests/fixtures/limited_popular_library_selected_handoff_dry_run/p30-t5-limited-popular-libraries.example.json
```

The run records exactly three selected candidates:

- `flask.core`
- `gin.core`
- `docc2context.core`

It also keeps the six P30 deferred candidates out of selected handoff:

- `xyflow.workspace`
- `xyflow.react`
- `xyflow.svelte`
- `xyflow.system`
- `cupertino.core`
- `navigation_split_view.core`

## Boundary

The artifact remains `producer_preview_evidence_only`. It records selected
candidate evidence, producer preflight status, static viewer status, required
evidence roles, and `registryAcceptanceDecision.status: external_required`.
It is not SpecPM acceptance.

It does not accept packages, accept relations, seed baselines, remove
`preview_only`, publish registry metadata, or create a SpecPM pull request.

See also <doc:SelectedCandidateHandoffProposal> and
<doc:LimitedPopularLibrarySelectedHandoffDryRun>.
