# Next Task: P25-T1 Package Set Contract Alignment

**Status:** Selected
**Last Archived:** P24-T1 Harvested Spec Quality Depth
**Archived:** 2026-06-05

## Recently Archived

- `P24-T1` upgraded generated package specs from safe metadata previews to
  subject-focused preview contracts with deterministic summary and evidence
  improvements.

## Motivation

- SpecPM now defines package-set concept, relation vocabulary, exact search
  semantics, draft registry metadata shape, SpecHarvester monorepo discovery
  handoff, multi-package producer intake, and the `xyflow` reference scenario.
- SpecHarvester needs an implementation plan aligned to those contracts before
  emitting multi-package monorepo bundle sets.

## Goal

- Document how the SpecPM package-set contracts map to SpecHarvester
  implementation tasks for workspace inventory, package-set candidates, scoped
  member candidates, relation proposals, bundle-set preflight, viewer output,
  and an `xyflow` smoke/reference scenario.

## Next Step

Start `P25-T1` as a planning/alignment task before implementing runtime
monorepo discovery.
